{
  "name": "@gitgraph/graph-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@gitgraph/core": "workspace:*",
    "d3-force": "^3.0.0",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "@types/d3-force": "^3.0.10",
    "vitest": "^2.1.0"
  }
}
