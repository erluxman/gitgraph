{
  "name": "@gitgraph/smoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --serve",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@gitgraph/core": "workspace:*",
    "@gitgraph/graph-renderer": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
